```mermaid
flowchart TD
    A[応募者] -->|リンクで飛んで応募| B[応募フォーム]
    B -->|手入力| C[採用管理システム]
    C -->|メール送信リクエスト| D[メール送信サービス]
    D -->|外部マネージドサービスで送信| E[メール]

    C --> F[採用担当者]

    %% コメントボックス
    B -.->|自動連携は最初はしない<br>(直接手入力)| C
    D -.->|外部のマネージドサービスを使用| E

    %% スタイル
    classDef dashed stroke-dasharray: 5;
    C:::dashed
```